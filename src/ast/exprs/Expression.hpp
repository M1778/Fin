#pragma once
#include "../nodes/ASTNode.hpp"

namespace fin {
// Base Expression class is already defined in ASTNode.hpp but we might include it here for consistency or just rely on ASTNode.hpp
// Actually it is already in ASTNode.hpp.
}
