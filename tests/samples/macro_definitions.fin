//@ normative
//@ ok
/* [WIP]
import { Collection } from collection::std;
import { HashMap } from hashmap::std;


// Hygienic Rust-like macro definition (NOT DECIDED YET)
@macro my_vec {
    () => {
        new Collection::<int>{}
    };
    ($($x:expr),*) => {
        {
            let temp <auto> = new Collection::<int>{};
            $(temp.add($x);)*
            temp
        }
    }
}

@macro map {
    ($($key:expr => $val:expr),*) => {
        {
            let temp <auto> = new HashMap::<string, int>{};
            $(temp.put($key, $val);)*
            temp
        }
    }
}

fun main() <void> {
    let v <auto> = my_vec![1, 2, 3];
    let m <auto> = map!{ "a" => 1, "b" => 2 };
}

*/
